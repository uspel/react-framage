import "./style";

import Framage from "./component";
import { useFramageAnimation, useFramageImage } from "./hooks";
import { FramageProps, FramageView, FramageNineslice, FramageAnimation } from "./types";

export { Framage as default, Framage, useFramageAnimation, useFramageImage, FramageProps, FramageView, FramageNineslice, FramageAnimation };
